<Window x:Class="GameBox.Games.MinesweeperGame"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="💣 Minesweeper" Height="650" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="#BDBDBD">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#757575" Padding="15" Margin="10,10,10,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="💣 Minesweeper" 
                           FontSize="22" FontWeight="Bold" Foreground="White" 
                           VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0">
                    <TextBlock Text="Mines: " FontSize="16" Foreground="White" 
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="MinesText" Text="10" FontSize="16" FontWeight="Bold" 
                               Foreground="#FF5252" VerticalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="20,0">
                    <TextBlock Text="Flags: " FontSize="16" Foreground="White" 
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="FlagsText" Text="0" FontSize="16" FontWeight="Bold" 
                               Foreground="#FFD700" VerticalAlignment="Center"/>
                </StackPanel>
                
                <Button Grid.Column="3" Content="New Game" 
                        Click="NewGame_Click"
                        Background="#4CAF50" Foreground="White"
                        BorderBrush="#45A049" BorderThickness="2"
                        Padding="15,8" FontSize="14" FontWeight="Bold"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#66BB6A"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <!-- Game Area -->
        <Border Grid.Row="1" Margin="10" Background="White" 
                BorderBrush="#757575" BorderThickness="2">
            <Grid x:Name="GameGrid" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
